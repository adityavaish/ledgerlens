# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json
name: ledgerlens
metadata:
  template: ledgerlens@1.0.0
services:
  app:
    project: .
    language: js
    host: appservice
    docker:
      path: Dockerfile
      context: .
infra:
  provider: bicep
  path: infra
  module: main
